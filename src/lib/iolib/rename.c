/*
   =============================================================================
	rename.c -- rename a file
	Version 2 -- 1987-06-29 -- D.N. Lynx Crowe
   =============================================================================
*/

rename (old, new)
     char *old, *new;
{
  char buff[60];

  return (-1);			/* return an error for now */
}
